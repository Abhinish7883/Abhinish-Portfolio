{
  "name": "abhinish-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:prod": "vite build --mode production",
    "build:staging": "vite build --mode staging",
    "build:dev": "vite build --mode development",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "firebase:login": "firebase login",
    "firebase:init": "firebase init",
    "deploy:prod": "npm run build:prod && firebase use production && firebase deploy",
    "deploy:staging": "npm run build:staging && firebase use staging && firebase deploy",
    "deploy:dev": "npm run build:dev && firebase use dev && firebase deploy",
    "deploy:all": "npm run build:prod && firebase use production && firebase deploy && npm run build:staging && firebase use staging && firebase deploy && npm run build:dev && firebase use dev && firebase deploy"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-error-boundary": "^4.0.12",
    "react-helmet-async": "^2.0.5",
    "react-icons": "^5.5.0",
    "react-router-dom": "^6.22.0",
    "@emailjs/browser": "^4.4.1",
    "framer-motion": "^12.7.4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.14",
    "eslint": "^8.0.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "postcss": "^8.4.23",
    "tailwindcss": "^3.3.0",
    "vite": "^6.3.2",
    "vite-plugin-compression": "^0.5.1"
  }
}

